import React from 'react';
import './App.css';
import './walletcomponent.css';; // Create a separate CSS file for the navbar styles

const Navbar = () => {
  return (
    <div className="navbar">
      <span className="navbar-text">Synced</span>
    </div>
  );
};

export default Navbar;